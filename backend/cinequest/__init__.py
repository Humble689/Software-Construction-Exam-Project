"""CineQuest Django project package.

This initializer marks ``cinequest`` as a Python package and is loaded when
the project package is imported.

Keep package-wide startup hooks here only when truly necessary
(for example, Celery app auto-discovery setup).
"""

